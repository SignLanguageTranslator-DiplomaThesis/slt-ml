from setuptools import setup, find_packages

setup(
    name='slt',
    version='1.0',
    author='Filip Denisa; Ivan Alexandru',
    description='ML model for Sign Language gesture recognition and classification',
    packages=find_packages(),
)
